fastapi==0.104.1
uvicorn==0.24.0
sqlalchemy==2.0.23
pydantic==2.5.0
pydantic-settings==2.1.0
speedtest-cli==2.1.3
python-dotenv==1.0.0
alembic==1.12.1
apscheduler==3.10.4
httpx==0.25.2
psutil>=5.9.0
websockets>=12.0              # required for FastAPI WebSocket support with uvicorn
sentry-sdk[fastapi]>=1.39.2   # optional — only activates when SENTRY_DSN env var is set
redis>=5.0.0                  # optional — for caching and ML predictions
twilio>=8.0.0                 # optional — for SMS alerts (smart_alerts.py)
scikit-learn>=1.3.0           # Isolation Forest anomaly detection + ML predictions
numpy>=1.24.0                 # statistical analysis
openai>=1.0.0                 # LLM chatbot (GPT-4o-mini) — set OPENAI_API_KEY env var
groq>=0.4.0                   # LLM chatbot fallback (llama-3.1-8b) — set GROQ_API_KEY env var
# psycopg2-binary removed — now using Turso (libSQL) via httpx HTTP adapter
